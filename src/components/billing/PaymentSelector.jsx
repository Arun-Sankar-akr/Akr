import {Banknote,Smartphone,CreditCard} from "lucide-react"; const methods=[["cash","Cash",Banknote],["upi","UPI",Smartphone],["card","Card",CreditCard]]; export default function PaymentSelector({value,onChange}){return <div className="payment-grid">{methods.map(([v,l,I])=><button key={v} className={`pay ${value===v?"active":""}`} onClick={()=>onChange(v)}><I size={18}/>{l}</button>)}</div>}