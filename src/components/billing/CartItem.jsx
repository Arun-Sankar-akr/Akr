import {Minus,Plus,Trash2} from "lucide-react"; import {formatCurrency} from "../../utils/currency"; export default function CartItem({item,onQty,onRemove}){return <div className="cart-row"><div><b>{item.name}</b><span>{formatCurrency(item.sellingPrice)} / {item.unit}</span></div><div className="qty"><button onClick={()=>onQty(item.id,-1)}><Minus size={13}/></button><b>{item.quantity}</b><button onClick={()=>onQty(item.id,1)}><Plus size={13}/></button></div><strong>{formatCurrency(item.quantity*item.sellingPrice)}</strong><button className="delete" onClick={()=>onRemove(item.id)}><Trash2 size={15}/></button></div>}