import {Plus} from "lucide-react"; import {formatCurrency} from "../../utils/currency"; export default function ServiceSelector({services,onAdd}){return <div className="service-grid">{services.map(s=><button className="service-card" key={s.id} onClick={()=>onAdd(s)}><small>{s.category}</small><b>{s.name}</b><strong>{formatCurrency(s.sellingPrice)} / {s.unit}</strong><Plus size={16}/></button>)}</div>}