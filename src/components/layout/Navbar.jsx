import { Bell, Menu, Sun, Moon } from "lucide-react"; import { useTheme } from "../../context/ThemeContext"; export default function Navbar({ onMenu }) { const { theme, toggleTheme } = useTheme(); return <header className="topbar"><button className="menu-btn" onClick={onMenu}><Menu /></button><div className="top-title"><span>Business workspace</span><b>Shop Management Console</b></div><div className="top-actions"><button className="icon-btn" onClick={toggleTheme}>{theme === "light" ? <Moon size={17} /> : <Sun size={17} />}</button><button className="icon-btn"><Bell size={17} /></button><div className="status-dot"><i /> Live</div></div></header> }